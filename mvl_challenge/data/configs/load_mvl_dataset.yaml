scene_dir: None
scene_list: None

runners:
  mvl:
    data_dir: ${scene_dir}
    scene_list: ${scene_list}
    batch_size: 10
    size: -1
    grid_size: 0.01 
    min_likelihood_percent: 0.000001
    padding: 20
    max_room_factor_size: 2
    num_workers: 5
